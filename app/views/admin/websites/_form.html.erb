<%= form_with(model: admin_website) do |form| %>
  <% if admin_website.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_website.errors.count, "error") %> prohibited this admin_website from being saved:</h2>

      <ul>
        <% admin_website.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.select :theme_id, Admin::Theme.all.collect { |p| [ p.name, p.id ] }  %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
