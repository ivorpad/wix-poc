<%= form_with(model: admin_theme) do |form| %>
  <% if admin_theme.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_theme.errors.count, "error") %> prohibited this admin_theme from being saved:</h2>

      <ul>
        <% admin_theme.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :is_enabled, style: "display: block" %>
    <%= form.check_box :is_enabled %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
