<%= form_with(model: admin_section) do |form| %>
  <% if admin_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(admin_section.errors.count, "error") %> prohibited this admin_section from being saved:</h2>

      <ul>
        <% admin_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.rich_text_area :content %>
  </div>

  <div>
     <%= form.select :admin_theme_id, Admin::Theme.all.collect { |p| [ p.name, p.id ] }  %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
